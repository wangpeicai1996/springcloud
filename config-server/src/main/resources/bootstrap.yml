spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wangpeicai1996/config-repo
          username: www.wangpeicai@qq.com
          password: 970326ling
          basedir: /Users/wangpeicai/worksapce/workspace_idea/springcloud/config/basedir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"
